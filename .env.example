# Copy to .env and fill in real values. .env is git-ignored.

# AWS credentials for Amazon Bedrock (used by openai_practices.py to
# generate rotated product views via Nova Canvas). Leave unset if you
# don't use that script.
aws_region=eu-west-1
aws_access_key_id=
aws_secret_access_key=
aws_session_token=
